#!/usr/bin/env bash
# Lint all shell scripts with shellcheck.
set -euo pipefail

REPO="$(cd "$(dirname "$0")/.." && pwd)"

shellcheck "$REPO/ci-run.sh" "$REPO/ci-rsync.sh" "$REPO/sci" \
    "$REPO/ci-setup.sh" "$REPO/ci/smoke" "$REPO/ci/e2e"
echo "shellcheck passed"
